export type ContactPropsType = {
    contacts: ContactInformationType[]
}

export type ContactInformationType = {
    url: string,        // Correspond à l'URL du site
    image: string         // Correspond pas à l'image dans le Git
}